[project]
name = "duckeb-docs"
version = "0.1.0"
description = "Notebook-first DuckDB workflow docs and templates"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "duckdb>=1.5.4",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
