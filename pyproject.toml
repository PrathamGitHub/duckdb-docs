[project]
name = "duckeb-docs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "duckdb>=1.5.4",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
